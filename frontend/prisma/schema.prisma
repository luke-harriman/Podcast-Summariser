datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model users {
  full_name String
  email    String   @unique
  password String
}

model creator_lists {
  email           String    @unique
  creator_link    String
  creator_name    String
}